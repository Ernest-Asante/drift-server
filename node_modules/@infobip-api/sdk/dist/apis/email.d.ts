import { Http } from '../utils/http';
import { InfobipAuth } from '../utils/auth';
import { EmailDomain } from './email-domain';
declare class Email {
    http: Http;
    status: any;
    report: any;
    log: any;
    domain: EmailDomain;
    constructor(credentials: InfobipAuth);
    send(email: any): Promise<any>;
    validate(to: string): Promise<any>;
    get(bulkId: string): Promise<any>;
    reschedule(bulkId: string, sendAt: string): Promise<any>;
    private getReport;
    private getLog;
    private getStatus;
    private updateStatus;
}
export { Email };
//# sourceMappingURL=email.d.ts.map